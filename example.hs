myPlus a b = a + b

sq b = b * b

isPositive x = if x >= 0
                then "positive"
                else "negative"

isPeter name = if name == "peter"
                then "Peter"
                else "Someone else"
